import Container from '../layout/Container';

const Contact = () => {
  return (
    <section id="contact" style={{ padding: 'var(--spacing-24) 0', borderBottom: '1px solid var(--color-border)' }}>
      <Container>
        <h2>Contact Placeholder</h2>
      </Container>
    </section>
  );
};

export default Contact;
