import Container from '../layout/Container';

const HeroPlaceholder = () => {
  return (
    <section id="heroplaceholder" style={{ padding: 'var(--spacing-24) 0', borderBottom: '1px solid var(--color-border)' }}>
      <Container>
        <h2>Hero Placeholder Placeholder</h2>
      </Container>
    </section>
  );
};

export default HeroPlaceholder;
