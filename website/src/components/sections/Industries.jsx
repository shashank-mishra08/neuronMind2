import Container from '../layout/Container';

const Industries = () => {
  return (
    <section id="industries" style={{ padding: 'var(--spacing-12) 0 var(--spacing-24)', borderBottom: '1px solid var(--color-border)' }}>
      <Container>
        <h2>Case Studies Placeholder</h2>
      </Container>
    </section>
  );
};

export default Industries;
