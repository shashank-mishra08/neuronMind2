import Container from '../layout/Container';

const Industries = () => {
  return (
    <section id="industries" style={{ padding: 'var(--spacing-24) 0', borderBottom: '1px solid var(--color-border)' }}>
      <Container>
        <h2>Industries Placeholder</h2>
      </Container>
    </section>
  );
};

export default Industries;
